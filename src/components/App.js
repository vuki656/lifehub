import React from "react";
import { withRouter } from "react-router-dom";

function App() {
    return (
        <div className="App">
            <p>dashboard</p>
        </div>
    );
}

export default withRouter(App);
