export type LoadingSpinnerProps = {
    loaderColor: 'blue' | 'white',
    loaderVariant: 'button' | 'fullScreen',
}

export type VariantType = {
    size: string,
    spinnerWidth: string,
    minHeight?: string,
}
