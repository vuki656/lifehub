import React from 'react'

export const Login: React.FunctionComponent<{}> = () => {
    return (
        <p>Login page</p>
        // TODO rename register form css to form so it can be reused in login
    )
}
